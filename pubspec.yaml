name: clean_arch_lint
description: Custom lint rules for enforcing Clean Architecture boundaries in Flutter/Dart projects.
version: 1.3.0

repository: https://github.com/saulogatti/clean_arch_lint

environment:
  sdk: ">=3.13.0 <4.0.0"

# Add regular dependencies here.
dependencies:
  analysis_server_plugin: ^0.3.22
  analyzer: ^14.3.0

  path: ^1.9.1

dev_dependencies:
  lints: ^6.1.0
  test: ^1.32.0
